services:
  rss-generator:
    build: .
    ports:
      - "8001:8000"
    volumes:
      - ./data:/app/data       # Persist SQLite DB and HTTP cache
    env_file:
      - .env
    restart: unless-stopped
